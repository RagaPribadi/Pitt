-- This will create the DB table for our example message board.
-- by the way, comments in SQL are usually created with a double-hyphen.
CREATE TABLE t_survey (
  c_color VARCHAR(64),
  c_text TEXT
);
